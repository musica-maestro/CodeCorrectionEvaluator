/* SPECIFICA (max 1.5):....0
   TIPO PROBLEMA (max 1):..1
   FUNZIONE (max 6):.......0  //******** MANCANTE
   MAIN PROGRAM (max 2.5):.0
   TOTALE..................1
*/
#include <stdio.h>


/*SPECIFICA
 *input: leggere gli interi introdotti dall'utente e memorizzarli in un array  //****** IN INPUT CI SONO GLI INTERI E UN INTERO CHE E' LA DIMENSINE DELL'ARRAY -0.5
 *Pre-Condizione: ogni tripla di elementi consecutivi deve contenere esattamentew due elementi maggiori o uguali a 5 ed almeno due elementi pari
 //******* CONFONDI PRE E POST CONDIZIONE -0.5
 *output: la funzione restituisce 1  //******* SEMPRE 1? -0.3
 *Post-Condizione: se la funzione è falsa invece restituisce 0*/

 /*TIPO DI PROBLEMA: verifica universale*/

 int main() {
	 int lunghezza;
	 while(i=0; i<lunghezza; i++)  //****** A LUNGHEZZA NON E' ASSEGANTO NESSUN VAOLRE -1
	 //****** QUESTO WHILE NON CI VA
		 printf("Inserisci la lunghezza della sequenza:\n");
		 scanf("%d", &lunghezza);

	int sequenza[lunghezza];
	while(i=0; i<sequenzas[lunghezza];i++)		// ***** CONFRONTO ITERATORE E ELEMENTO -0,75
		printf("inserisci il numero intero %d", i+1);
		scanf("%d", &sequenza[i]);		// ****** GRAFFE -0,25

		//******* QUI DOVEVI RICHIAMARE LA FUNZIONE -0.5
		printf("vero\n");
		else   //******* ELSE SENZA IF   -0.5

		printf("falso\n");

	return =0;
}
