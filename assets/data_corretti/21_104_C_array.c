/* SPECIFICA (max 1.5):....0 //******** MANCA
   TIPO PROBLEMA (max 1):..0 //******** MANCA
   FUNZIONE (max 6):.......0
   MAIN PROGRAM (max 2.5):.0
   TOTALE..................
*/

#include <stdio.h>

//*funzione che verifica l'esistenza di una tripla di elementi consecutivi*//
int compresiMultipli3(int interi[], int lunghezza[]){
	//pre: lunghezza è la lunghezza dell'array

int modulo       //tripla di elementi consecutivi


/*verificare se ci sono almeno due elementi il cui valore è compreso tra -10 e 10*/
'while'(-10<=interi[]>=10) //****** NON E' SINTASSI C


/*verificare l'esistenza di due elementi multipli di 3*/
'intero'[i]=0
'intero'[j]=0
'while'(interi[]%3==0)


/*verificare l'esistenza di una tripla di elementi consecutivi*/
'if'(-10<=interi[]>=10, interi[]%3==0)
	modulo==1
'else'
	modulo==0




int main(int sequenza, int lunghezza)
{  //****** LE VARIABILI NON SONO DICHIARATE -1
	/*input*/
	printf("Caro utente, quanti interi ha la sequenza?\n");
	scanf("%s", &sequenza) //***** IL FORMATO PER LEGGERE INTERI E' %d -0.5

	printf("Caro utente introduci gli interi della sequenza.\n");
	scanf("%s", &sequenza)  //***** E' NECESSARIO UN CICLO PER ACQUISIRE GLI ELEMENTI -1
}

	/*output*/
	compresimultipli3(int sequenza, int lunghezza)
	if(-10<=interi[]>=10; intero[i]%3==0; intero[j]%3==0) //**** NO!
		printf("Caro utente, esiste una tripla di elementi.\n")
	else
		printf("Caro utente, non esiste una tripla di elementi.\n")


	return 0;
}
