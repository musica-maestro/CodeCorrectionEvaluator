/* SPECIFICA (max 1.5):....0  //******** MANCANTE
   TIPO PROBLEMA (max 1):..0
   FUNZIONE (max 6):.......0  //******** NON C'E' NIENTE
   MAIN PROGRAM (max 2.5):.0  //******** MANCANTE
   TOTALE..................
*/
#include <stdio.h>

/*Algoritmo che verifica se, data una sequenza di interi, ogni tripla di interi consecutivi, contiene due elementi maggiori o uguali a 5*/
//problema di verifica esistenziale// //****** E' UNA VERIFICA UNIVERSALE -1

int	funzionemaggiori5Pari(int *arr, int len)
{
	int i;
	int check;

	check = 1;
	i = 0;
	while (arr[i] != '\0')  //***** NO, NON E' UNA STRINGA
	{
		if(i = len)
			break;
		arr[]
		i++;
	}

}

int main()
{

}