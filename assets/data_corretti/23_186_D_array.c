/* SPECIFICA (max 1):......0
   TIPO PROBLEMA (max 1):..0
   FUNZIONE (max 6):.......0
   MAIN PROGRAM (max 2):...0
   TOTALE (10).............0
*/
/*verificare se all'interno della sequenza esiste una tripla di elementi in cui uno dei tre
è il resto della divisione degli altri due*/

/*input: sequenza di interi//******** MANCA LA LUNGHEZZA -0.2
pre-condizioni: verificare se all'interno della sequenza esiste una tripla di elementi //*** NO -0.3
output: uno dei tre è il resto della divisione degli altri due //******* l'OUTPUT E' UN BOOLEANO -0.2
post-condizioni: divisione tra elementi di tipo int //******** POST CONDIZIONE NON CORRETTA -0.3
TIPO DI PROBLEMA: VERIFICA UNIVERSALE */  //*** NO, E' ESISTENZIALE -1


#include <stdio.h>

int restoDivisione (int seq, int lungh[i]) //**** NON C'E' NIENTE!

{
	int i=0;
	if ((i%i+1%==i+2 && i+1%i+2%==i), || i+2%i%==i+1)
		else ((i+1%i+2%==i+3 && i+2%i+3%==i+1), || i+3%i+1%==i+2)

	{
	printf("quanti numeri vuoi inserire\n");
	scanf("%d", i);
	}

}

int main()
{
	printf("inserisci la sequenza di numeri\n", );
	scanf ("%d%d%d%d%d")


	return 0;
}